# Secrets / env
.env
.env.*
.dev.vars
.dev.vars*
!.dev.vars.example
!.env.example

# Wrangler
.wrangler/

# Build output (n/a for now — pure static)
dist/
build/

# npm-package/ — npm package for @ajinkyakulkarni/cost-calc-mcp
# Committed: npm-package/package.json, npm-package/bin/ (static, never change)
# Generated by scripts/copy-engine.mjs (run before npm pack/publish):
npm-package/dist/
npm-package/mcp/
npm-package/node_modules/
npm-package/*.tgz
npm-package/package-lock.json

# Node (not used yet, but in case we add tooling)
node_modules/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Local outputs / scratch
outputs/
__pycache__/

# Claude Code local instructions — repo-shared instructions live in
# CLAUDE.md (committed); machine-local notes live in CLAUDE.local.md
# (never committed) and may reference private sister repos, internal
# infra endpoints, or credentials that don't belong in a public README.
CLAUDE.local.md
*.local.md

# Playwright artifacts
test-results/
playwright-report/
docs/eie-calibration-2026-06.md
docs/eie-instrumentation-spec.md
bench/docs/
